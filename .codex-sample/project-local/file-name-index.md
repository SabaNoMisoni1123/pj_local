# {{ プロジェクト名称 }} ファイル名索引

作成日: YYYY-MM-DD

目的: 今後のCodex作業で、プロジェクト内の資料を素早く探すための索引。ファイル名・フォルダ名・一部Markdown内容から整理したものであり、外部リンク先やOffice/PDFは本文確認しない

保存方針: プロジェクト固有情報のため、共通 `.codex/` ではなく `project-local/` に保存する。Teams URL、パスコード、秘密URL、個人連絡先は転記しない。

## 全体構成

| フォルダ | 役割 | 主な参照タイミング |
|---|---|---|
| `project-local/` | Codex用プロジェクト固有メモ | ファイル索引、プロジェクト文脈、作業ログ |

## 入口ファイル

| ファイル | 役割 | 注意 |
|---|---|---|
| `AGENTS.md` | 作業ルール。日本語回答、生成物も日本語、派生成果物は新規ファイル、プロジェクト固有情報は `project-local/` に保存 | 最初に確認する |

## 主要タスク領域

## 会議メモ

| ファイル（パス） | 内容の目安 |
|---|---|
|  |  |

## 補助メモ・その他

| ファイル（パス） | 内容の目安 | 注意 |
|---|---|---|
|  |  |

## 参照禁止ディレクトリ

次の条件に該当するディレクトリ配下は、ファイル名・内容・メタデータを確認せず、根拠や推測材料にしない。

- `old/`、`_old/` 等、人間が旧版・不採用情報として退避したディレクトリ。

| ディレクトリ | 除外理由 |
|---|---|
|  |  |

## 低信頼・注意ファイル

次の条件に該当するものは、単独根拠にしない。

- ファイル名・フォルダ名に `_gpt`、`_tmp`、`_draft`、`_sample`、`_export` を含むもの。
- `gpt/` 配下のファイル。
- `codex_*.md`、`*_codex_review.md` などの生成・派生成果物。
- `~$` で始まるOffice一時ファイル。
- HTML化・抽出済みファイル。原本Markdown、PDF、docx、xlsx等との関係を確認して使う。
- ショートカット `.lnk`。参照先の存在や内容は未確認。
