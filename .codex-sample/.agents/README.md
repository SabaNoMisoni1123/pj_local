# .agents

`.agents` は、移植先プロジェクトで使うエージェント向け命令セットです。

- 常時適用ルールはルート `AGENTS.md` に置きます。
- `.agents/AGENTS.md` は、このディレクトリ全体の設計・索引・保守方針です。
- `.agents/skills/*/SKILL.md` は、特定タスクでのみ使う再利用ワークフローです。

新しいスキルを追加する前に、既存の `SKILL.md` の `name` と `description` を確認し、発火条件が重複しないことを確認してください。
