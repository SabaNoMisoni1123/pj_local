# AGENTS.md

日本語で回答してください。生成物もすべて日本語で作成します。
Answer in Japanese. All output will also be in Japanese.

## 本環境の目的

本環境は、シンクタンク・コンサルティング業の研究員が、アサインされるプロジェクトごとに作成するローカル環境でのプロジェクトフォルダ（以降「プロジェクトフォルダ」）で Codex を運用する際の `AGENTS.md` や `.codex` ディレクトリを作成することを目的としています。

その他にもフロントエンド開発やpythonを用いたツール開発も想定します。

## 本環境のアウトプット

再配布・移植する雛形としてのアウトプットは `.codex-sample` ディレクトリに集約します。その他のディレクトリはローカルのプロジェクトフォルダのフォルダ構成を参考までに示しており、原則として変更しません。

ただし、`.codex-sample` 自体を整備するための監査結果、調査レポート、再編検討メモなど、再配布向きではない本環境の保守資料は `.codex-sample` の外に保存します。

## `.codex-sample` の位置づけ

`.codex-sample` 内のファイルは、将来、ローカルのプロジェクトフォルダ（業務遂行するためのフォルダ）に `.codex` ディレクトリや `AGENTS.md` として保存・移植される雛形です。

そのため、本環境そのものの作業意図、検討方針、Codexへの指示は `.codex-sample` ではなく、このルートの `AGENTS.md` に記録します。

`.codex-sample` には、プロジェクトフォルダで業務遂行支援に使うための汎用的な運用ルール、業務カット、skill、テンプレートだけを作成・編集します。

本環境では、`.codex-sample` 内に新規ファイルを作成し、また既存ファイルを編集することで、業務遂行支援に使える `.codex` ディレクトリおよび `AGENTS.md` の雛形を整備します。

## `.codex-sample` への配置判断

`.codex-sample` は再配布・移植される雛形だけを置く場所です。ファイルを追加・更新する前に、それが「移植先プロジェクトでそのまま使う業務遂行支援の部品」かを確認します。

`.codex-sample` に保存してよいものは、原則として以下に限ります。

- 移植先プロジェクトのルートに置く `AGENTS.md` の雛形。
- Codex 設定の雛形。現状は `.codex-sample/config.toml` に置き、移植時は `.codex/config.toml` として扱う。
- 移植先で使う汎用的な業務運用ルール、業務カット、テンプレート。
- Agent Skills として再利用する `.agents/skills/*/SKILL.md` と、その skill 実行に必要な `references/`、`scripts/`、`assets/`、`evals/`。
- 新規導入直後に空で使う `project-local/` の雛形。ただし、案件情報を記入した後の `project-local/` は再配布対象外。

`.codex-sample` に保存しないものは、以下です。

- 本環境そのものの開発方針、検討メモ、作業ログ、監査結果、再編提案、調査レポート。
- `.codex-sample` の品質確認や改善のためだけに作成したレポート。
- MCP、外部ツール、公開リポジトリ、ライブラリ等の調査結果で、移植先の業務遂行時に常時参照する運用ルールではないもの。
- 実案件の人物、組織、ファイル索引、メール要約、TODO、URL、資格情報、作業メモ。
- Codex のセッション、ログ、キャッシュ、DB、一時ファイル、認証情報。

再配布向きではないが本環境の保守に必要な資料は、`.codex-sample` の外に置きます。たとえば、監査レポート、MCP 推奨調査、公開 skill リポジトリ調査、再編検討メモは、ルート配下の `docs/` など本環境用の管理領域に保存します。

既存の `.codex-sample` 内に再配布向きでないファイルを見つけた場合は、原則として内容を評価したうえで `.codex-sample` 外へ移す提案を行います。単に削除せず、必要なら移動先と移植対象から除外する理由を明記します。

## 文字コード

- 新規作成・編集するテキストファイルは、原則として UTF-8（BOM なし）で保存する。
- 特に `SKILL.md` は UTF-8 BOM 付きで作成・保存しない。
- PowerShell でファイルを書き出す場合、BOM が付かない方法を選ぶ。既存ファイルの先頭に `EF BB BF` が付与されていないか、必要に応じて確認する。

## `SKILL.md` frontmatter

- `SKILL.md` の YAML frontmatter は、原則として `name` と `description` のみで構成する。
- `description` は YAML パースエラーを避けるため、原則としてダブルクォートで囲む。特に `Japanese triggers:` など、値の中にコロンを含める場合は必ずクォートする。
- `description` 内でダブルクォートを使う必要がある場合は、別表現に置き換えるか YAML として正しくエスケープする。
- `SKILL.md` を追加・編集した後は、frontmatter の `name`、`description`、親ディレクトリ名の整合、未クォートのコロン、UTF-8 BOM の有無を確認する。
